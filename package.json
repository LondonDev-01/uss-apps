{
  "name": "uss-apps",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@11.16.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
