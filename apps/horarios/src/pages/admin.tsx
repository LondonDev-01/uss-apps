import React from 'react'

export default function Admin(){
  return (
    <div style={{padding:20}}>
      <h2>Admin (removido)</h2>
      <p>El panel de administración local fue eliminado. Usa la consola de Neon o accesos directos a la base de datos para gestionar usuarios y claves.</p>
    </div>
  )
}
